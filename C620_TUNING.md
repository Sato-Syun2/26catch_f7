# C620単独調整の作業記録

## 保存・復帰

- 元の `../26catch_f7` は `Yasaki_Control_dev` のRobstride調整中のまま保持。stash、reset、上書きはしていない。
- 本作業は別worktree `26catch_f7_c620_tuning` / `c620_tuning_20260909`。
- 基準: `Yasaki_Control_dev` 6e72145、C620設定元: `origin/ohmori/disturbance_dev` 9067042。
- 接続F7: NUCLEO-F767ZI、ST-LINK `066DFF485570854967104926`。
- 書込み前Flash 2MiB: `debug_logs/c620_baseline_20260909/flash_before.bin`。
  SHA256: `b5a4bc72d2e4faba877850dfb04e7603674512b290f005b3388466485233c490`。
- 元のソース調整へ戻る場合は `../26catch_f7` へ移動するだけ。ボード上のファームは別途戻す必要がある。
  保存binを書き戻す際はこのST-LINKを指定し、先頭アドレス `0x08000000`、照合付きで行う。
  **元ファームは起動後の自動校正があるため、復元・リセットで動き得る。自動復元はしない。**
- USB-LAN `enx9c69d3b99922` へ一時NetworkManager接続 `c620-tuning-20260909` を追加。
  192.168.5.100/24、既定経路なし、永続保存なし。既存接続は変更していない。
  終了時は `nmcli connection down c620-tuning-20260909` で解除できる。

## ピン・再生成

両ブランチのIOC/ピン定義/GPIO/CANピン設定は同一。
書込み前の実バイナリでも校正引数はGPIOA、pin=16 (PA4)、NO=0、速度=-10と確認した。

| 用途 | IOCと生成コード |
| --- | --- |
| C620 CAN2 RX | PB12 / AF9 |
| C620 CAN2 TX | PB6 / AF9 |
| 原点入力 sensor2 | PA4 / GPIO Input / Pull-up / High検出 |

Disturbanceの「sensor3」コメントは実引数と不一致。統合コードはsensor2と明記した。
ピンを推測して変更しない。将来の変更はIOCと生成コードを同時に更新する。
既存CAN3生成コードのTransmitFifoPriority=ENABLEに対しIOCにTXFPが欠けていたため補完した。
CubeMXそのものによる再生成は未実施。`python3 tools/test_c620.py` で設定整合を検査する。

## 制御・試験仕様

完成版ではユーザー希望により起動時自動校正を有効にする。
`C620Commission.h` の `C620_AUTO_CALIBRATE_ON_BOOT` を完成確認後に1へ変更すること。
現時点は校正停止原因が未解決なので0。起動後1秒以上・CAN受信確立後に一度だけ校正し、
成功後は停止、異常時はラッチ停止。起動待ち中のDisableでも自動始動をキャンセルする。
端点探索は自動化しない。完成時は実測済み可動域を保存・適用する対応も必要。

- C610=0台、Robstride=0台、C620=1台、ID3。CW、座標換算125/36*2をDisturbanceから移植。
  座標の実長との一致はまだ未検証。
- 起動時停止。校正は `c620_calibrate`、1A伸長は `c620_survey` サービスでのみ始動。
- CAN送信直前の通常電流指令上限2.5A、校正だけ10A/探索1A。
  ユーザーがLiPo未接続・電源能力不足の可能性を挙げ、校正10Aを明示指定したため変更。
  電源能力不足が原因と確認できたわけではなく、電源側1A/24Wへ合わせる制御ではない。
  当初の校正1Aは固着停止。ユーザーからスイッチが硬いとの情報・続行指示を受け2Aで再試験。
  ホスト側は校正時のCAN電流返送値10.5A超過、通常時2.5A超過で中断する。
- 校正: 速度PIで縮み方向-10座標単位/s、電流上限10A。
  速度指令を最初の0.5秒で0から増加、試験は最大10秒。
  ユーザー指示で校正の固着検出を廃止。
  PA4 Highで原点確定・Disable。速度絶対値30超で異常停止。
  通信断・時間・範囲保護は維持。直接10A校正の急加速後、ユーザーの了承を得て変更。
- 探索: 速度PIで+40座標単位/s（0.5秒ランプ）、既定電流上限1A、今回だけ上限5A。
  `c620_survey` のdata/試験ツールの `--survey-current 5` で今回分だけ指定。
  再起動後は既定1A。通常制御2.5A上限には影響しない。
  最大60秒、速度150座標単位/s・移動1000単位を超えたら異常停止。
  始動1秒後以降、1秒の停滞かつCAN報告電流が上限の80%以上で
  ゼロ指令停止して端候補を記録。障害物と機械端はソフトだけでは区別できない。
- CANフィードバック10ms超の途絶で停止。通常制御/探索はROS指令100ms途絶で停止。
  校正はROS指令待ちではなくF7上のスイッチ・固着・時間制限で管理。
- 探索結果から5単位以上内側の範囲を `c620_range` で設定するまで通常制御不可。
- Mode4=DOB、Mode5=MPC+DOB。ID4専用の可動域・825mm/sをC620には適用しない。
  C620暫定速度上限10、alpha=5、DOB帯域5、kp=.05、ki=.1。
  J=.001、d=.01、Kt=1は正規化された暫定モデルであり、M3508の同定済み物理値ではない。
- `c620_current/kp/ki/dob/alpha` は停止中だけ設定可能。RAM設定なので再起動で初期値に戻る。

## 現時点の実機結果

Debug/Releaseビルドと、実CANパック経路を使うホスト回帰テストを実施。
書き込み照合済みDebug ELFのSHA256:
`248eed153abbe4f2e9438d73d76123750126bb9739893312d5c78ee80bb829eb`。

最初はC620電源OFFでCAN全ID受信ゼロ・ACKエラー。電源ON後はID3の受信とROS約100Hzを確認。
`debug_logs/c620_calibrate_20260909_235643/` の校正試験は約0.078単位の変位後に固着停止。
実測電流最大1.166A（指令上限1A）、PA4はLowのまま。校正未完了、故障停止ラッチ・Disable。
機械状態/スイッチを確認するまで繰り返し押し付けたり電流を上げたりしない。
ユーザーから「試験時はスイッチを押していた、今は離した」と回答あり。
押下時GPIOA IDR=0xDC6D、解放後=0xFC6DでPA4はいずれもLow。
差分bit13はSWDIOであり、原点スイッチの変化とは認定できない。
その後の15秒入力読み取りでもPA3/PA4/PA5の変化なし。
`tools/c620_gpio_probe.py` は停止中の配線特定用で、リセット/書込をしない。
実際の原点信号の接続ピン・押下/解放レベルの確認が再開条件。
追記: `c620_gpio_20260910_000248.jsonl` の60秒監視で、ユーザーの押下/解放操作に
対応するPA4だけのHigh/Low遷移を確認。PA3/PA5はHighのまま。
ユーザーが「離している」と回答した状態ではPA4=Lowで安定。
したがって配線PA4・押下Highは既存設定と一致し、ピン/極性の変更は不要。
00:04の再試行 `c620_calibrate_20260910_000421/` でも校正未完了。
約0.554単位の変位後に固着停止。実測電流最大1.245A（指令上限1A）、
速度最大絶対値13.31単位/s。最終Disable確認。機械端接触か途中固着かは未確認。
00:09の2A校正 `c620_calibrate_20260910_000910/` は約6.007単位まで進んだが、
スイッチ未検出・固着停止。実測電流最大2.119A。次段階は校正2.3A、探索1Aを維持。
00:10の2.3A校正 `c620_calibrate_20260910_001038/` もスイッチ未検出・固着停止。
試験中の変位約3.52単位、実測電流最大2.402A。Disable確認。指定上限2.5Aで最終確認する。
2.5A上限の速度校正版は書込み・照合のみで、駆動前にユーザーから方針変更あり。
「縮み方向へ1A直接、固着検出なし、LimitSwitchで校正完了」との指示を受け変更。
00:14の直接1A試験 `c620_calibrate_20260910_001442/` は59.99秒・6000点を記録。
固着判定では停止せず60秒上限まで通電。位置変化は最大0.0441座標単位、
実測電流平均0.9988A、最大1.7004A。PA4未検出のため校正未完了。
終了後Disable=true、実測電流0.0024A、速度0を確認した。
書込み・照合済みファームは `debug_logs/c620_fixed1a_firmware_20260910/firmware.elf`、
SHA256 `edc65438bbd7ad9cbb73e92ce11e8ace6c981781529320ae72b92cf3abb2eb5e`。
現在はこのファームの異常停止ラッチ状態。自動校正フラグは引き続き0。
1A伸長による端点確認、DOB/MPCの実機チューニングはまだ未完了。

実機試験ツール: `python3 tools/c620_measure.py inspect|calibrate|survey`。
各試験はCSV/JSONを `debug_logs/` に保存し、動作試験の終了・例外時にDisableする。

### 00:31 10A校正試験・5A終点確認の保留

ユーザー指定により校正だけ10A（0.5秒ランプ、最大10秒）、今回の終点探索だけ
`survey --survey-current 5` を許容。通常制御上限2.5A、探索既定1Aは維持。
Debug/Release・ホスト回帰試験を通過し、書込み照合済みELF SHA256は
`4daf12b72da46ed63bcd0ecf60676557b6cf7b66d66d07eecc6bf4c9dc6c6a7f`。
`c620_calibrate_20260910_003148/` ではランプ途中で急加速し、
CAN報告電流4.0625A、速度-152.55座標単位/sで速度保護停止。
これは10A到達・校正成功ではない。ホストも速度保護で例外終了しDisable応答あり。
終了直後の残留速度でホストの停止確認ループも例外になったため、別途
`c620_inspect_20260910_003212/` でFAULT(s=6)、Disable(state=0)、速度0、
最終位置-12.3961、PA4=Lowを確認した。5A終点探索は実行していない。
起動自動校正は引き続き無効（完成後有効化の要望は未完了）。
CAN報告電流は電源側電流の独立計測ではない。00:19の1A再試験時、ユーザーは
24V電源の表示増加を約0.1Wと報告。電源能力不足が原因とはまだ確定していない。

### 00:36 速度PI・10A上限で原点校正成功

ユーザーが目視・電源遮断可能と回答し、速度制限付き10A上限の校正を承認。
速度指令-10座標単位/s（0.5秒ランプ）、速度30超停止、最大10秒、固着停止なし。
Debug/Release・回帰テスト（電流飽和、速度追従時の電流低下、過速度停止を含む）通過。
書込み照合済みELF SHA256:
`778c20de60e307685bdb6ab59effde7676dfd3598a4709e0196d3bcbed71ea29`。
`debug_logs/c620_calibrate_20260910_003617/` は約8.76秒でPA4検出・CALIBRATED(s=2)。
CAN報告電流最大5.0024A、速度範囲-22.9167～11.9213座標単位/s。
終了後Disable=true、速度0。原点オフセット設定後の停止位置は0.4866。
10A固定通電の試験ではなく、電流上限10Aの速度PI校正が成功した。
5A終点探索・DOB/MPC実機調整は未実施。自動起動校正は完成確認待ちで0のまま。

### 00:38 5A終点探索中断・電源交換まで調整休止

ユーザー指定で `survey --survey-current 5` を実行。
`debug_logs/c620_survey_20260910_003849/`：開始時CALIBRATED、PA4 High。
伸長開始後約0.05秒で速度155.09座標単位/sとなり、速度保護で停止。
終点候補の取得前であり、機械的終点は未確認。電流指令5Aを電源側実測5Aとは扱わない。
Disable応答後、残留速度によりホスト確認ループも例外となったため、別途
`debug_logs/c620_inspect_20260910_003901/` でFAULT(s=6)、出力無効(state=0)、
速度0、停止位置5.8924、PA4 Lowを確認した。この位置は終点として登録しない。
ユーザー希望により電源交換までDOB/MPCの実機パラメータ調整を休止。
通常電流上限2.5A、起動自動校正0、元Robstride作業ツリー保存は維持。
再開時は再校正が必要。終点探索は急加速を避ける速度制御化を検討する。

### 00:42 速度PIで終点候補を取得・目視確認待ち

ユーザーが電源遮断可能と回答。探索を速度PI・電流上限5Aへ変更し、
Debug/Releaseビルド、電流上限・速度追従・探索過速度/通信断/時間上限の回帰テスト通過。
書込み照合済みELF SHA256:
`df13b45e127c3ab520380c996448369b7dd98000ea504c167d01c14e9b79ddbc`。
`c620_calibrate_20260910_004206/` で原点を再取得。
`c620_survey_20260910_004220/` で約3.63秒伸長、位置19.3456で停滞しCONTACT(s=4)。
停止直前CAN報告電流-4.9854A、速度0。電流絶対値のログ最大5.1270A。
出力無効化後に戻り動作があり、最終位置16.8245・速度0・Disable=true。
ログ全体の速度範囲-35.3009～17.7083（負側ピークはDisable後）。
機械端への到達はユーザーの目視確認待ちで、途中抵抗との区別は未確定。
通常可動域は登録していない（max=0）。座標単位の実長との一致も未検証。
電源交換までDOB/MPC調整を休止。起動自動校正は完成待ちのため引き続き0。

### 00:47 途中停滞からの続行試験

ユーザーが19.35は機械端ではないと確認。可動域には採用しない。
明示的なc620_surveyをCONTACTからも受理し、原点を維持した再開を追加。
再開時は旧候補を破棄。FAULTからの再開は禁止のまま。
停滞検出時間を0.3秒から1秒へ延長。回帰テスト・Debug/Releaseビルド通過、書込み照合済み。
ELF SHA256 `cbb70aae69447eb079849526deb89f8584c39d10927239b3ace8fee2cc09b74a`。
今回の書換えで原点が失われるため `c620_calibrate_20260910_004735/` で再校正後、
`c620_survey_20260910_004751/` で5A上限・速度10で伸長した。
再び19.36で停滞停止。出力無効後17.0890まで戻り、速度0・Disable=trueを確認。
前回をほぼ越えられず、機械端ではないというユーザー確認を覆す証拠はない。
可動域未登録、終点未確認。電流増加や停滞中の無期限通電は実施していない。

### 00:57 指定40/s・上限5A探索

ユーザー指定で探索指令を40座標単位/sへ変更。探索固有の30/s停止を除去し、
共通150/s停止・通信断・60秒上限・1秒停滞停止を維持。校正/通常制御設定は変更なし。
Debug/Releaseと回帰テスト通過・書込み照合済みELF SHA256:
`0e4a0cdb937cfca449db7d8ba8d3214749023144a47fba03dcf806dcdc2213ee`。
初回はPC側micro_ros_agent終了により通信なし、駆動前に中断。
`ros2 run micro_ros_agent micro_ros_agent udp4 --port 8888` を起動して復旧。
`c620_calibrate_20260910_005658/` で再校正成功。
`c620_survey_20260910_005713/`：約2.52秒駆動、最大速度41.4352、位置34.25で停滞停止。
出力無効後31.0652まで戻り、速度0・Disable=true。
前回19.36を越えたが、34.25が機械端かは目視確認待ち。可動域未登録(max=0)。
電源交換までDOB/MPC調整は休止のまま。
