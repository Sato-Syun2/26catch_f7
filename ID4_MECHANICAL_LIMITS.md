# RoboMaster ID4：真の機構上限と保護設定

## 真の機構上限（手動実測）

**528.878173828125 mm（表示値528.878mm）**

2026-09-09、原点スイッチで校正後にDisableし、ユーザーが手で機構限界まで
動かした位置をROS FBで測定した。直近1秒間変動なし、速度0、Disableを確認。
校正直後の停止位置は-0.091309mm。この値を差し引かず、ROS/F7の校正座標を採用。
以前の自動探索500.688mmと503.841mmは真の機構上限として使用しない。

## コード側の設定元

`canlib_robomaster/CAN_RoboMas/id4_velocity_safety.h`

- `ID4_MECHANICAL_MAX_MM`: 528.878173828125
- `ID4_POSITION_COMMAND_MAX_MM`: 520.0（含む）
- `ID4_HARD_STOP_MAX_MM`: 526.878173828125（機構端より2mm手前）
- `ID4_HARD_STOP_MIN_MM`: -2.0（原点停止誤差を許容。負側機構端の実測値ではない）
- 非常停止判定距離: `0.065 × |実測速度|` mm
- 通常速度包絡: `(ハード境界までの距離 - 2mm) / 0.10秒`

位置指令0..520mmは許可する。位置・通常速度・DOBの目標速度を残距離から制限。
全モード（電流モードを含む）で実測速度と予測停止距離を監視し、越える場合は
速度ゼロへの制動に切り替え、停止後Disable・再Enable禁止ラッチにする。
実位置がハード境界に達した場合も直ちに電流ゼロ・Disable・ラッチ。
ラッチ解除は再起動。校正中は未確定座標のため位置境界判定から除外する。
ID4は校正終了後Disableのままにする。ID1の制御設定は変更していない。

校正は±2A（速度PIDとCAN送信直前の両方）。ユーザー許可による段階試験後、
通常制御は±10A、Mode5の速度指令上限825mm/s（無負荷500rpm×99mm/60）。
Mode4は従来の800mm/s制限を維持。ID4のみで、ID1は変更しない。
端探索経路は`id4_endpoint_survey=false`で無効化。

## 検証状態と制約

ホスト単体テストは520mmの受理、範囲外拒否、減速包絡、異常値を確認。
Mode5実機試験では825mm/s設定の220→520mmが約0.628秒で±0.5mm以内に入り、
最終偏差約0.266mm。非常減速は通常PIではなくDOBの速度ゼロ指令で行う。
以前の一定加速度100/500mm/s²包絡は通常軌道を途中停止させたため撤廃し、
DOBの実測停止距離に余裕を加えた時間包絡へ変更した。
保護は負荷変動を含む数学的保証ではない。電流上限を下げる場合は再検証が必要。
CAN断線・電源断では能動制動を保証できない。位置FB破損も別途対策が必要。

2026-09-09、ユーザーの書き込み許可後、校正のみ2Aに変更して検証付き書き込み済み。
初回起動で未確立FBが保護をラッチする問題が発生したため、実際の校正完了後のみ
保護を有効化するよう修正し、Debug/Release再ビルド・再書き込みした。
F7 RAM上で校正完了=true、校正中=falseを確認。ROSでも原点付近約0.040mm、
速度0、Disableを確認。ID1もDisable。その後Mode5実機試験を実施済み。
詳細は `debug_logs/id4_mpc_tuning_20260909/README.md` を参照。
